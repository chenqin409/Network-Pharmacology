BiocManager::install("AnnotationDbi")
install.packages("BiocManager")
BiocManager::install("org.Hs.eg.db")
library("BiocManager")
library("org.Hs.eg.db")          #引用包
BiocManager::install("org.Hs.eg.db", type = "source", update = FALSE, ask = FALSE)
Sys.which("make")
BiocManager::install("org.Hs.eg.db", type = "source", update = FALSE, ask = FALSE)
BiocManager::install("AnnotationDbi")
library("BiocManager")
library("org.Hs.eg.db")          #引用包
BiocManager::install(c("XVector", "Seqinfo", "Biostrings"),
type = "source", update = FALSE, ask = FALSE)
BiocManager::install("org.Hs.eg.db", type = "source", update = FALSE, ask = FALSE)
library("BiocManager")
library("org.Hs.eg.db")          #引用包
inputFile="Drug_Disease.txt"     #输入文件名称
outFile="id.txt"                 #输出文件名称
setwd("C:\\Users\\chenq\\My Work\\Network Pharmacology\\7.GO和KEGG富集分析\\1.GO富集分析")          #设置工作目录
rt=read.table(inputFile,sep="\t",check.names=F,header=F)       #读取文件
genes=as.vector(rt[,1])
entrezIDs <- mget(genes, org.Hs.egSYMBOL2EG, ifnotfound=NA)    #找出基因对应id
entrezIDs <- as.character(entrezIDs)
out=cbind(rt,entrezID=entrezIDs)
colnames(out)=c("symbol","entrezID")
write.table(out,file=outFile,sep="\t",quote=F,row.names=F)
install.packages("colorspace")
install.packages("stringi")
install.packages("ggplot2")
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("DOSE")
BiocManager::install("clusterProfiler")
BiocManager::install("org.Hs.eg.db")
BiocManager::install("enrichplot")
library("clusterProfiler")
library("org.Hs.eg.db")
library("enrichplot")
library("ggplot2")
pvalueFilter=0.05         #p值过滤条件
qvalueFilter=0.05         #矫正后的p值过滤条件
setwd("C:\\Users\\chenq\\My Work\\Network Pharmacology\\7.GO和KEGG富集分析\\1.GO富集分析")             #设置工作目录
rt=read.table("id.txt",sep="\t",header=T,check.names=F)     #读取id.txt文件
rt=rt[is.na(rt[,"entrezID"])==F,]                           #去除基因id为NA的基因
gene=rt$entrezID
#定义颜色类型
colorSel="qvalue"
if(qvalueFilter>0.05){
colorSel="pvalue"
}
#GO富集分析
kk=enrichGO(gene = gene,OrgDb = org.Hs.eg.db, pvalueCutoff =1, qvalueCutoff = 1, ont="all", readable =T)
GO=as.data.frame(kk)
GO=GO[(GO$pvalue<pvalueFilter & GO$qvalue<qvalueFilter),]
#保存富集结果
write.table(GO,file="GO.txt",sep="\t",quote=F,row.names = F)
#定义显示Term数目
showNum=10
if(nrow(GO)<30){
showNum=nrow(GO)
}
#柱状图
pdf(file="barplot.pdf",width = 10,height =12)
bar=barplot(kk, drop = TRUE, showCategory =showNum,split="ONTOLOGY",color = colorSel) + facet_grid(ONTOLOGY~., scale='free')
print(bar)
dev.off()
#气泡图
pdf(file="bubble.pdf",width = 10,height =12)
bub=dotplot(kk,showCategory = showNum, orderBy = "GeneRatio",split="ONTOLOGY", color = colorSel) + facet_grid(ONTOLOGY~., scale='free')
print(bub)
dev.off()
install.packages("colorspace")
install.packages("stringi")
install.packages("ggplot2")
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("DOSE")
BiocManager::install("clusterProfiler")
BiocManager::install("enrichplot")
BiocManager::install("pathview")
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("DOSE")
BiocManager::install("clusterProfiler")
BiocManager::install("enrichplot")
BiocManager::install("pathview")
library("clusterProfiler")
library("org.Hs.eg.db")
library("enrichplot")
library("ggplot2")
library("pathview")
pvalueFilter=0.05         #p值过滤条件
qvalueFilter=0.05         #矫正后的p值过滤条件
setwd("C:\\Users\\chenq\\My Work\\Network Pharmacology\\7.GO和KEGG富集分析\\2.KEGG富集分析")          #设置工作目录
rt=read.table("id.txt",sep="\t",header=T,check.names=F)    #读取id.txt文件
rt=rt[is.na(rt[,"entrezID"])==F,]                          #去除基因id为NA的基因
colnames(rt)[1]="Gene"
gene=rt$entrezID
#定义颜色类型
colorSel="qvalue"
if(qvalueFilter>0.05){
colorSel="pvalue"
}
#kegg富集分析
kk <- enrichKEGG(gene = gene, organism = "hsa", pvalueCutoff =1, qvalueCutoff =1)
KEGG=as.data.frame(kk)
KEGG$geneID=as.character(sapply(KEGG$geneID,function(x)paste(rt$Gene[match(strsplit(x,"/")[[1]],as.character(rt$entrezID))],collapse="/")))
KEGG=KEGG[(KEGG$pvalue<pvalueFilter & KEGG$qvalue<qvalueFilter),]
#保存富集结果
write.table(KEGG,file="KEGG.txt",sep="\t",quote=F,row.names = F)
#定义显示Term数目
showNum=30
if(nrow(KEGG)<showNum){
showNum=nrow(KEGG)
}
#柱状图
pdf(file="barplot.pdf",width = 9,height = 12)
barplot(kk, drop = TRUE, showCategory = showNum, color = colorSel)
dev.off()
#气泡图
pdf(file="bubble.pdf",width = 9,height = 12)
dotplot(kk, showCategory = showNum, orderBy = "GeneRatio",color = colorSel)
dev.off()
keggId="hsa05417"       #选择通路
geneFC=rep(1,length(gene))
names(geneFC)=gene
pv.out=pathview(gene.data = geneFC, pathway.id = keggId, species = "hsa", out.suffix = "pathview")
